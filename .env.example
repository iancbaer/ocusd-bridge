ETH_RPC_URL=https://mainnet.infura.io/v3/YOUR_KEY
ETH_CUSTODY_CONTRACT=
OCTRA_RPC_URL=https://your-octra-node.example/rpc
OCTRA_TOKEN_CONTRACT=
RELAYER_OCTRA_ADDRESS=

# The Ethereum relayer key signs withdrawal approvals and submits withdraw() txs.
RELAYER_PRIVATE_KEY_ETH=

# The Octra relayer key submits mint() transactions.
RELAYER_PRIVATE_KEY_OCTRA=

POLL_INTERVAL_MS=3000
CONFIRMATIONS_REQUIRED=2
RELAYER_DB_PATH=relayer.sqlite
OCTRA_CALL_FEE=1000

# Self-funding / reserve controls.
# 10 bps = 0.1%. The relayer mints/releases net amount and leaves fees in custody reserve.
BRIDGE_FEE_BPS=10

# Minimum native Octra balance, in Octra fee units, to keep on the relayer wallet.
# If relayer balance is below MIN_OCTRA_RELAYER_BALANCE_OU + OCTRA_CALL_FEE,
# Ethereum deposits remain pending instead of spending the last Octra mint fee.
MIN_OCTRA_RELAYER_BALANCE_OU=0

# Deployment only.
DEPLOYER_PRIVATE_KEY=
RELAYER_SIGNER_ADDRESS=
INITIAL_OWNER_ADDRESS=
USDT_ADDRESS=0xdAC17F958D2ee523a2206206994597C13D831ec7

# Optional Octra adapters while Octra's public signed-tx schema is settling.
# OCTRA_EVENTS_ENDPOINT can expose Burned events as JSON for the relayer.
# OCTRA_SUBMIT_ENDPOINT can be a local signer endpoint that submits contract calls.
OCTRA_EVENTS_ENDPOINT=
OCTRA_SUBMIT_ENDPOINT=

# Withdrawal mode.
# Default (unset or 0): relayer auto-submits custody.withdraw() on Ethereum — users just burn on Octra.
# Set to 1 to require users to submit their own withdraw() via MetaMask (legacy mode).
USER_SUBMITTED_WITHDRAWALS=0

# Proof page
PROOF_PORT=8080
